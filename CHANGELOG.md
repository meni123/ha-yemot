# היסטוריית שינויים

## 1.1.2

### אבטחה
- **סינון IP עמיד בפני זיוף:** הבדיקה עברה מקריאת `X-Forwarded-For` ישירות אל `request.remote`,
  כתובת ה-TCP האמיתית. ההדר נקרא רק דרך מנגנון `trusted_proxies` של Home Assistant, לאחר אימות.
- **רשימת היתר לפעולות:** רק דומיינים ופעולות המוגדרים ב-`const.py` ניתנים להפעלה מרחוק.
  קריאות ל-`shell_command`, `homeassistant.restart` וכדומה נחסמות גם עם טוקן תקין.
- **טוקן אקראי:** נוצר ב-`secrets.token_urlsafe(24)` בעת ההתקנה. ברירת המחדל `123456` הוסרה.
- **השוואה בזמן קבוע** באמצעות `hmac.compare_digest`.
- **טוקן שגוי מוחזר כ-401** ומדווח ל-`process_wrong_login`, לחיבור למנגנון חסימת ה-IP של HA.
- **סינון אופציונלי לפי מספר מתקשר** דרך פרמטר `ApiPhone`.

### תיקוני באגים
- זיהוי ערכי תאריך ושעה לא עבד כלל בגלל השוואה לאות `t` קטנה. הוחלף ב-`dt_util.parse_datetime`.
- טעינה מחדש של האינטגרציה קרסה עקב רישום כפול של נתיב ה-HTTP. ה-View נרשם פעם אחת בלבד
  וההגדרות נקראות דינמית.
- זיהוי יחידת המידה תפס כל יחידה שהכילה את האות c (למשל count) והקריא אותה כמעלות.
- `&` הומר למחרוזת שהכילה מקף, מיד לאחר שכל המקפים הוסרו.
- הוספת חסימת מופע כפול של האינטגרציה באמצעות `unique_id`.
- נרמול סלאש עודף בסוף הכתובת החיצונית.

### שיפורי ביצועים
- המתנה לשינוי מצב אמיתי דרך `async_track_state_change_event` במקום `sleep(2.5)` קבוע.
  זמן התגובה בשיחה ירד מ-2.5 שניות לכ-200 מילישניות במקרה הטיפוסי.
- שימוש ב-session המשותף של HA במקום יצירת `ClientSession` בכל קריאה.
- הסרת `sleep(0.5)` בין יצירת השלוחה להעלאת הקובץ, לטובת בדיקת תגובה.

### שיפורי ממשק
- תרגום מלא לעברית של מסכי ההגדרה, כולל הסבר לכל שדה.
- מסך הגדרות (Options Flow) לעריכה ללא צורך במחיקה והתקנה מחדש.
- אימות טוקן הניהול מול שרתי ימות בזמן ההגדרה. שני פורמטי הטוקן נתמכים.
- בדיקת תקינות תגובות ה-API והצגת שגיאות ברורות באוטומציה.
- תמיכה ב-`ttsVoice` וב-`ttsRate` בשירות השיחה הקולית.
- מילון תרגום מצבים מורחב, עם נפילה חזרה למצב הגולמי במקום "לא מוכר".

### תשתית
- `manifest.json` הושלם: `iot_class`, `documentation`, `issue_tracker`, `dependencies`.
- הוספת בדיקות hassfest ו-HACS ב-GitHub Actions.
- הוספת קובץ רישיון MIT.

### שינוי שובר תאימות
משתמשים מגרסאות קודמות יקבלו טוקן אבטחה חדש. יש להריץ מחדש את `ha_yemot.create_extension`
עבור כל שלוחה קיימת.
